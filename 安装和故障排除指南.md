# See.Net 安装和故障排除指南

## 证书验证错误解决方案 (错误代码 0x800B010A)

### 问题说明
此错误通常出现在尝试安装MSIX应用包时，因为应用程序没有有效的代码签名证书。

### 推荐解决方案

#### 方案1：使用自包含版本（强烈推荐）
**这是最简单和最可靠的方法，无需任何证书配置。**

1. 找到自包含发布文件夹：
   `
   D:\Projects\DevTools\See.Net\See.Net\bin\Release\net10.0-windows\win-x64\publish
   `

2. 直接运行 See.Net.exe：
   - 双击 See.Net.exe
   - 或者使用项目根目录的"启动See.Net.bat"脚本

3. 创建快捷方式：
   - 右键点击 See.Net.exe
   - 选择"发送到" -> "桌面快捷方式"

#### 方案2：安装测试证书（如果必须使用MSIX包）

1. 安装测试证书：
   - 双击打开：See.Net_TestCert.pfx
   - 选择"本地计算机"
   - 选择"将所有证书放入下列存储" -> "受信任的根证书颁发机构"
   - 输入密码：See.Net123

2. 重启计算机

3. 再次尝试安装MSIX包

#### 方案3：启用Windows开发者模式

1. 打开"设置" -> "更新和安全" -> "开发者选项"
2. 启用"开发人员模式"
3. 启用"旁加载应用"
4. 重启计算机

### 系统要求

#### 自包含版本
- Windows 10 或更高版本 (64位)
- 4GB 或更多内存
- 500MB 可用磁盘空间
- WebView2 运行时（通常已预装）

#### MSIX版本
- Windows 10 版本 1809 或更高版本
- 以上所有要求
- 需要有效的代码签名证书或启用开发者模式

### 常见问题

**Q: 为什么推荐使用自包含版本而不是MSIX包？**
A: 自包含版本不需要证书签名，更简单可靠，且功能完全相同。

**Q: 自包含版本会占用更多空间吗？**
A: 不会，两者大小相近（约184MB），都包含必要的运行时文件。

**Q: 我可以在多台计算机上使用同一个自包含版本吗？**
A: 可以，只需将整个publish文件夹复制到目标计算机即可运行。

**Q: 如何卸载自包含版本？**
A: 直接删除整个文件夹即可，不会在系统中留下残留。

**Q: 以后能否升级到有签名的MSIX版本？**
A: 可以，当有正式的代码签名证书后，可以重新打包为MSIX版本。

### 技术支持

如遇到其他问题，请检查：
1. Windows Defender 或其他安全软件是否阻止了程序运行
2. 确保下载的文件完整，没有损坏
3. 检查Windows更新，确保系统为最新版本
4. 如果是网络问题，可以尝试离线安装

### 文件清单

**自包含版本包含：**
- See.Net.exe (主程序)
- 259个DLL文件（运行时和依赖库）
- webassets 文件夹（Office和Markdown预览资源）
- 多语言支持文件夹
- README.md（用户手册）

**测试证书文件：**
- See.Net_TestCert.pfx（自签名测试证书）
- 密码：See.Net123

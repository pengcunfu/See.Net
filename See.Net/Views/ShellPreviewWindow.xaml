<Window x:Class="See.Net.Views.ShellPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:See.Net.Views"
        xmlns:vm="clr-namespace:See.Net.ViewModels"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" Width="960" Height="680"
        WindowStartupLocation="CenterScreen" PreviewKeyDown="OnPreviewKeyDown"
        ResizeMode="NoResize">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:TextContentViewModel}">
            <views:TextView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HexContentViewModel}">
            <views:HexEditorView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ImageContentViewModel}">
            <views:ImageView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InfoContentViewModel}">
            <views:InfoView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:OfficeContentViewModel}">
            <views:OfficeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MarkdownContentViewModel}">
            <views:MarkdownView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:WebContentViewModel}">
            <views:HtmlView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AudioContentViewModel}">
            <views:AudioView/>
        </DataTemplate>
    </Window.Resources>
    <Border CornerRadius="8" Background="White" BorderBrush="#D0D0D0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- 标题栏，支持拖拽 -->
            <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="14,8"
                    MouseLeftButtonDown="OnTitleBarDrag" Cursor="SizeAll">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="{Binding Preview.Title}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Preview.Subtitle}" FontSize="11" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding PositionText}" Foreground="Gray" FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <Button Content="在 See.Net 中打开" Command="{Binding OpenInSeeNetCommand}" Padding="10,3" Margin="0,0,8,0" FontSize="11"/>
                        <TextBlock Text="Esc 或空格关闭" Foreground="Gray" FontSize="11" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Button x:Name="PinButton" Content="&#xE718;" FontFamily="Segoe MDL2 Assets" FontSize="12" Padding="8,2" 
                                Margin="0,0,8,0" Click="OnTogglePin" ToolTip="切换置顶"/>
                        <Button Content="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" Padding="8,2" Click="OnCloseClick" ToolTip="关闭预览"/>
                    </StackPanel>
                </Grid>
            </Border>
            <ContentControl Grid.Row="1" Content="{Binding Preview.Content}" Focusable="False"/>
        </Grid>
    </Border>
</Window>
